import 'package:flutter/material.dart';

class GenericUserCard extends StatelessWidget {
  
  const GenericUserCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
